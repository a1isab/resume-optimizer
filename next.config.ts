import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* You can delete output: 'export' and basePath. 
     Vercel handles everything automatically from the root domain! */
};

export default nextConfig;